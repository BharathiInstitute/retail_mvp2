/// Minimal placeholder after removing the original detailed GST demo seed.
/// Keeps existing buttons from breaking. Extend with real logic if needed.
Future<void> seedGstr3bDemoData() async {
  await Future<void>.delayed(const Duration(milliseconds: 200));
}
